import 'package:flutter/material.dart';

class LevelListScreen extends StatelessWidget {
  const LevelListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Level List Screen'),
      ),
    );
  }
}
